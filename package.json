{
  "name": "find-community",
  "version": "0.1.0",
  "private": false,
  "type": "commonjs",
  "scripts": {
  "build": "echo \"Build step placeholder (add transpile/minify here if needed)\"",
  "start": "node src/index.js",
  "dev": "node src/index.js",

  "lint": "eslint .",
  "lint:fix": "eslint . --fix",

  "test": "jest --runInBand",
  "test:ci": "jest --ci --reporters=default --reporters=jest-junit --coverage",

  "prepare:reports": "mkdir -p reports/security",
  "audit:json": "npm audit --json > reports/security/npm-audit.json || true"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-config-standard": "^17.1.0",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-n": "^16.6.2",
    "eslint-plugin-promise": "^6.6.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.0.0"
  },
  "jest-junit": {
    "outputDirectory": "reports/junit",
    "outputName": "junit.xml"
  }
}
